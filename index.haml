---
layout: default
---

.container
  %div
  {% for post in site.posts %}
    %span='{{ post.date | date: "%Y/%m/%-d" }}'
    %span.label.label-default="{{post.category}}"
    %h4
      %a(href='{{ post.url | prepend: site.baseurl }}')='{{ post.title }}'
  {% endfor %}