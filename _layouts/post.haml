---
layout: default
---

%section.col-md-8.col-md-offset-2
  %header
    %h1="{{ page.title }}"

    %p.post-meta='{{ page.date | date: "%b %-d, %Y" }}'
    %span.label.label-default="{{page.category}}"

  %hr

  %article="{{ content }}"